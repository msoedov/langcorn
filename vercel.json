{
    "devCommand": "uvicorn vercel:app --host 0.0.0.0 --port 3000",
    "builds": [
        {
            "src": "examples/vercel.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "examples/vercel.py"
        }
    ]
}
